<g:if test="${flash.error}">
    <font color="red"><i>${flash.error}</i></font>
</g:if>

<g:if test="${flash.info}">
    <font color="orange"><i>${flash.info}</i></font>
</g:if>

<g:if test="${flash.message}">
    <font color="green"><i>${flash.message}</i></font>
</g:if>